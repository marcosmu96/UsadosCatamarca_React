 <div className="card-container">

            <div className="loading">{!items.length && <section><span className="loader-93"> </span></section>}</div>

            {items.map(item => (

                <Item key={item.id} item={item} />

            ))}



            
        </div>